import { Radius } from '@/themes';

export const IconButtonTheme = { base: (theme: any, size = 40) => ({ width: size, height: size, borderRadius: Radius.full, alignItems: 'center', justifyContent: 'center', }), };
